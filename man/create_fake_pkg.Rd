% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_fake_pkg.R
\name{create_fake_pkg}
\alias{create_fake_pkg}
\title{Create a fake package}
\usage{
create_fake_pkg(
  name,
  path,
  action_on_failure = c("error", "warning", "message"),
  show_progress = FALSE
)
}
\arguments{
\item{name}{Character string with the name of the package.}

\item{path}{Character string with the path to the directory where the package
should be installed. See \code{Details}.}

\item{action_on_failure}{Character string indicating the action if package
installation fails: \code{"error"} (default) to throw an \link[=stop]{error},
\code{"warning"} to issue a \link{warning}, or \code{"message"} to show a \link{message}. See
\code{Details}.}

\item{show_progress}{\code{TRUE} or \code{FALSE}: show progress of the
\link[=install.packages]{installation}?}
}
\value{
A character string with the \link[=normalizePath]{normalised} path to the package
directory, returned \link[=invisible]{invisibly}. Identical to
\code{file.path(path, name)} if \code{path} is already normalized and points to a
writeable directory.
}
\description{
Create a minimal package for testing purposes.
}
\details{
\code{path} should \emph{not} include the package name.

If the package is already installed at the directory given by \code{path}, the
action taken by \code{action_on_failure} will be triggered.

The check if the package is \link[=requireNamespace]{functioning} correctly after
installation first looks for the package in the directory given by \code{path}. If
it is not found there, it will look for the package in the user directory
(with a warning) because \code{\link[utils:install.packages]{utils::install.packages()}} will ask if that
directory should be used if \code{path} does not point to a writeable directory.
If the packag is not functioning correctly, the action taken by
\code{action_on_failure} will be triggered.
}
\examples{
(path_to_pkg <- create_fake_pkg(name = "fakepkg",
                                path = file.path(".", "pkg_tests")))
# Failure if the package already exists in that directory
try(path_to_pkg <- create_fake_pkg(name = "fakepkg",
                                   path = file.path(".", "pkg_tests")))

}
