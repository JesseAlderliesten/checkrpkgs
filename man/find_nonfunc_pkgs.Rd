% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_nonfunc_pkgs.R
\name{find_nonfunc_pkgs}
\alias{find_nonfunc_pkgs}
\title{Find non-functional packages}
\usage{
find_nonfunc_pkgs(pkgs, quietly = FALSE)
}
\arguments{
\item{pkgs}{A character vector with names of packages to be checked, or
ending in such names, see \code{Details}.}

\item{quietly}{\code{TRUE} or \code{FALSE}: suppress warnings when loading
installed non-functional packages?}
}
\value{
A list of length two, with elements 'absent' and 'nonfunc' containing
character vectors with the names of packages in \code{pkgs} that are not installed
or are installed but non-functional, with a warning. The elements are
\code{character(0)} if all packages in \code{pkgs} are present, and if all packages
are installed and functional, respectively.
}
\description{
Function to check for non-installed or non-functional packages.
}
\details{
The part after the last forward or backward slash is considered to be the
package name if input to \code{pkgs} contains such slashes. Therefore the package
name, the file path to packages, and the full URL to packages from
\href{https://github.com/}{GitHub} can all be used as input to \code{pkgs}.

Packages are looked for in the library paths given by \code{\link[=.libPaths]{.libPaths()}}.
}
\section{Side effects}{

Packages are \link[=loadNamespace]{loaded}, such that \link[=update.packages]{updating}
them might fail. Restart \R to prevent such problems.
}

\section{Programming notes}{

This function uses \code{\link[=find.package]{find.package()}} and \code{\link[=requireNamespace]{requireNamespace()}} instead of
\code{\link[utils:installed.packages]{utils::installed.packages()}} because \code{installed.packages} does not check if
packages are functional, nor if all needed
\link[tools:package_dependencies]{dependencies} are installed and functional.
In addition, \code{installed.packages()} can be slow such that its
\link[=installed.packages]{help page} states that \code{\link[=requireNamespace]{requireNamespace()}} or
\code{\link[=require]{require()}} should be used instead.

Setting environment variable \verb{_R_TRACE_LOADNAMESPACE_} to a numerical value
(e.g., \code{Sys.setenv("_R_TRACE_LOADNAMESPACE_" = 4)}) will generate additional
messages on progress for non-standard packages, see the section \code{Tracing} in
\code{\link[=requireNamespace]{requireNamespace()}}.
}

\examples{
find_nonfunc_pkgs(pkgs = c("base", "grid"), quietly = FALSE)

non_existent_pkgs <- c("yz/wx/abcdef4", "wx/abcdef3", "abcdef2", "abcdef1")
find_nonfunc_pkgs(non_existent_pkgs, quietly = FALSE)
find_nonfunc_pkgs(non_existent_pkgs, quietly = TRUE)
find_nonfunc_pkgs(pkgs = c(non_existent_pkgs, "utils"), quietly = FALSE)

}
\seealso{
\link[tools:package_dependencies]{package_dependencies}\verb{(packages = "<pkgname>", recursive = TRUE)}
for dependencies and
\link[tools:dependsOnPkgs]{dependsOnPkgs}\verb{(pkgs = "<pkgname>", recursive = TRUE)}
for reverse dependencies; \code{tools::standard_package_names()} (present since
\R 4.4.0) for names of the base and recommended packages.

\code{\link[=old.packages]{old.packages()}} and \code{\link[BiocManager:valid]{BiocManager::valid()}} to check for outdated or too new
packages, where the latter takes the currently used version of Bioconductor
into account.

\code{options("defaultPackages")} for names of packages that are attached by
default when \R starts up; \code{\link[=loadedNamespaces]{loadedNamespaces()}} and \code{\link[utils:sessionInfo]{utils::sessionInfo()}}
for names of packages that are currently loaded.

The vignette \emph{Instructions about R packages}:
\code{vignette("r_pkgs", package = "checkrpkgs")}.

Other functions to get information about packages: 
\code{\link{get_details_pkgs}()}
}
\concept{functions to get information about packages}
